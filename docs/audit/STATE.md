# OmniFlow — Sentinel State Ledger (2026-07-21)

Running audit state so the loop survives context resets. Antigravity scaffolds; Claude (Sentinel) audits from disk each round, patches contained bugs, returns a verdict + redirection prompt; the user pilots Antigravity manually.

## Architecture baseline (immutable)
Kafka (KRaft) ← CockroachDB v24.3+ **JSON** changefeed → orchestrator. Transactional outbox + changefeed (no dual-write). Protobuf contracts + `buf.validate`. Go strict hexagonal. OTel W3C traceparent. WebGL 3D timeline on HLC `sequence_engine_key`. Power BI on CDC emit-time. Pure-Go **franz-go** is the standard Kafka client for new services (no CGO).

## Phase status
- **Phase 1** (infra/schema/proto): DONE, hardened.
- **Phase 2 CommBot: A-.** Hexagonal; consumer (confirmed-DLQ, retry, traceparent, protovalidate); LLM gateway (SSRF allowlist, x/time/rate token bucket, real parse); CRDB tx outbox + idempotency; `enable.auto.commit=false`. `ports` merged into `domain` (import-cycle fix).
- **Phase 3 Orchestrator: A-.** Kahn topo-sort + cycle→ErrTerminal; 1-tx CTE checkpoint (exactly-once outbox); `FOR UPDATE NOWAIT` lease (55P03→transient); re-fetch-under-lock quiet abort; HITL suspend (no ledger write) + resume via approval topic; decodes CRDB JSON changefeed envelope, skips `resolved`. Consumes `omniflow.orchestration.v1` + `omniflow.p2p.approval.v1`; emits `omniflow.p2p.completed.v1`.
- **Phase 4 Inventory Intelligence: A (COMPLETE).** Single-CRDB-tx ledger (real pgx.Tx: idempotency + lots + facts atomic); true FIFO drawdown + moving-avg; HLC late-arrival restatement = ResetLots + full-log replay via `applyMovement` (no recursion), facts `ON CONFLICT (event_id) DO UPDATE`; signed adjustments (depleteLots on negative); franz-go consumer (envelope decode, `errors.Is` transient via pgconn 40001/08*, confirmed-DLQ `ProduceSync`); SCD2 star schema + HLC-guarded MERGE; hardened CRDB→BQ changefeed (29092, topic_prefix, mvcc_timestamp, gc-protect). Files: `services/inventory-intelligence/**`, `infrastructure/{crdb_schema.sql,warehouse/{star_schema.sql,dax_measures.md}}`, `contracts/.../inventory/v1`.
- **Phase 5 Real-Time Viz: KICKED OFF (charter written, research+council done).** SSE gateway (franz-go) → Next.js/R3F flow-graph. Strategy: readable-first, 3D only for topology, numbers on flat HUD, time-box ~1wk, record early on seeded data, SSE-over-WS (documented), jitter-free via HLC watermark + client buffer + LERP/SLERP, InstancedMesh + refs-not-state. Leverage: scrub-back replay over the event log + business-semantic overlays. See `PHASE5_CHARTER.md`.

## LOCKED — do not touch
`services/commbot/**` logic, `dag.go`, `orchestrator_schema.sql`, `service.go` suspend logic, Phase-4 valuation/ledger.

## Make-It-Real progress (2026-07-23 round)
- **Prompt 1 (franz-go migration): DONE + VERIFIED.** Zero confluent/CGO; `CGO_ENABLED=0 go build ./...` = exit 0 on root AND services/viz-gateway; `go vet` clean. Uncommitted (baseline `388cc35`).
- **Prompt 2 (Dockerfiles): DONE.** All 5 services + `.dockerignore`. Not docker-build-verified (no local daemon). Uncommitted.
- **Prompt 3 (compose + crdb-init): BUILT by Antigravity, AUDITED + PATCHED, VERIFIED.** Full trail in `docs/audit/COMPOSE_PLAN_AUDIT.md` (rounds 1-3). Antigravity executed B1-B4 correctly (Go-side + compose all verified; both modules build exit 0). Sentinel patched 3 hard blockers in `infrastructure/init/crdb-init.sh` that Antigravity couldn't detect w/o local Docker (its "simulated boot" was fiction): C1 missing `--host=cockroachdb:26257` on every `cockroach sql` (init is a separate container → localhost hung forever); C2 in-script Kafka wait shelled non-existent `/opt/kafka/...` in the cockroach image → infinite loop (removed; compose depends_on service_healthy gates it); C3 guard query used non-existent `statement` column → fixed to `description`. compose lives at REPO ROOT (correct: `context: .` for Go Dockerfiles). Residual must-verify-in-CI: kafka-native healthcheck script path; `--accept-sql-without-tls` redundancy; the real E2E boot.
- **Prompt 4 (seed E2E): IMPLEMENTED by Claude-as-engineer (2026-07-23, post-crash), build+vet VERIFIED, boot only-in-CI.** Claude built it directly on disk rather than handing Antigravity another prompt (user mandate: "you are the engineer, you have all the power"). Deliverables (all uncommitted):
  - **Part C (projection made real):** `orchestrator_outbox` gained additive `sequence_engine_key INT8 NOT NULL` (Sentinel-authorized schema-lock exception); `state_store.go` CTE now `INSERT ... (..., sequence_engine_key) SELECT $5,'NodeTransition',$8,$9,$10` — the `insert_ledger` exactly-once guard `WHERE EXISTS (SELECT 1 FROM insert_ledger)` kept byte-for-byte; `wf.SequenceEngineKey` (uint64) is the source. `viz-gateway/internal/kafka/consumer.go handleP2PCompleted` rewritten to mirror `handleInventoryMovement`: unwrap `after{}`, skip `resolved`, read `after.aggregate_id/event_type/sequence_engine_key/occurred_at/trace_parent` via `extractString`/UseNumber (STRING, never float64). Deleted the bogus `po_id` + `seqKey="0"` mock.
  - **Part A (`tools/mock-llm/main.go` + Dockerfile):** ~60-line Go HTTP server, `POST /chat/completions` → `{"choices":[{"message":{"content":"1"}}]}` matching CommBot's `chatResponse`. Wired into compose (`mock-llm` service); commbot env got `LITELLM_URL=http://mock-llm:4000` + `OBJECT_STORE_HOSTS=storage.googleapis.com,raw.githubusercontent.com`. Used only by `email` seed mode.
  - **Part B (`tools/seed/main.go` + Dockerfile):** franz-go + pgx seeder, `SEED_MODE` = `outbox` (default) | `email`. **KEY ENGINEERING DECISION:** default seeds at the `commbot_outbox` TABLE (real classified VendorEmailReceived protobuf), so the REAL CRDB→Kafka changefeed drives orchestration.v1 → orchestrator → HITL suspend → completed → viz. This is because CommBot's LLM path is **un-runnable in CI**: `ClassifyIntent` fetches subject/body over https from an allowlisted, PUBLIC-IP-resolving host (SSRF guard in LOCKED `gateway.go`) BEFORE calling the LLM — impossible inside Docker. Seeder generates its own UUID+traceparent+seqKey(unixnanos), inserts outbox, **polls `workflows` until SUSPENDED** (deterministic proof stages 2-3 ran, avoids approval-vs-changefeed race), then produces `HumanApprovalEvent` to `omniflow.p2p.approval.v1` with the SAME event_id → orchestrator resumes → final_step → two orchestrator_outbox rows (approved + completed), both carrying seqKey. Prints `SEED_EVENT_ID=` / `SEED_SEQUENCE_ENGINE_KEY=`. `email` mode (opt-in) produces to communication.v1 and needs reachable allowlisted https URIs (e.g. raw.githubusercontent.com @ tested SHA) — documented, secondary.
  - **Part D (`scripts/e2e.sh`):** boot → `docker compose wait crdb-init`==0 → assert ≥3 running changefeeds → **open SSE curl BEFORE seeding** (gateway has NO backlog; late connect misses the live event — this was the latent bug in the old inline e2e.yml) → `go run ./tools/seed` → poll captured SSE ≤20s for the seqKey → dump logs on fail → always `down -v`. Skips cleanly (exit 78) if `CRDB_LICENSE` absent.
  - **`e2e.yml` refactored:** the 6 inline boot/seed/assert/teardown steps collapsed to one `bash scripts/e2e.sh` (single source of truth; fixes the connect-after-seed ordering bug).
  - **VERIFIED locally:** `CGO_ENABLED=0 go build ./...` + `go vet ./...` exit 0 on BOTH modules; `gofmt` clean on all new/changed files; `bash -n scripts/e2e.sh` clean; proto enum/field names confirmed against generated `.pb.go`; `.dockerignore` does not exclude `tools/`; no new go.mod deps. **NOT verified (no local Docker — honest):** the real container boot, changefeed emission, and SSE delivery. That runs in GitHub Actions once the repo is pushed + `CRDB_LICENSE`/`CRDB_ORG` secrets set. No boot log fabricated.
- **Prompt 5 (failure tests): BUILT by Antigravity, AUDITED + PATCHED by Sentinel, build/syntax VERIFIED, boot only-in-CI.** Antigravity delivered the `SEED_ACTION` refactor (`full`|`suspend-only`|`approve-only`, composes existing primitives — `full` behavior preserved), `scripts/failtest_killed_pod.sh`, `scripts/failtest_exactly_once.sh`, and two guarded CI jobs (`failtest-resume`, `failtest-exactly-once`). **Sentinel found + patched 4 blockers Antigravity couldn't catch without Docker (it verified syntax only):** (P1) wrong compose service name `crdb` → `cockroachdb` on every `docker compose exec` (both scripts); (P2) crdb-init readiness grep matched `"Init complete"` but the script prints `"crdb-init complete."` → never matched → 60s timeout → replaced with `docker compose wait crdb-init` + exit-code check (both scripts); (P3) killed-pod SSE curl hit `localhost:8080` (that's CRDB's admin UI) → viz is published on **8081** → fixed; (P4) **correctness:** exactly-once test queried `orchestrator_outbox WHERE aggregate_id='${SEED_EVENT_ID}'` but `aggregate_id` = workflow UUID (`wf.ID`), not event_id → always 0 rows → false failure → fixed to the `(SELECT id FROM workflows WHERE event_id=…)` subquery (mirrors the script's own ledger query). Also hardened the killed-pod EXIT trap (`SSE_PID=""` + guard) so an early failure still tears the stack down. Logic verified sound: killed-pod proves durable resume (final_step ledger count==1 after kill/restart/approve, workflow COMPLETED); exactly-once proves the duplicate approval no-ops via `service.go`'s `state != SUSPENDED` guard (outbox stays exactly 2 rows; per-partition key=event_id ordering makes it race-free). VERIFIED: both modules build+vet exit 0, `gofmt` clean, `bash -n` clean both scripts. NOT verified (no local Docker): the real kill/restart + duplicate-suppression under load — CI only. No boot log fabricated.
- **NEXT:** push repo to GitHub (public) + set `CRDB_LICENSE`/`CRDB_ORG` secrets → watch `e2e` + `failtest-resume` + `failtest-exactly-once` go green. Then Prompt 6 (FIFO late-arrival restatement — DEFERRED here because `inventory-intelligence` consumes `omniflow.p2p.completed.v1` with no SKU/quantity in the payload; that input path needs a grounding/repair pass first) + README/SCOPE.md, then the deferred WebSocket-upgrade prompt.
- **Superseded draft (2026-07-23, pre-implementation):**
  - `docs/antigravity/PROMPT_04_seed_e2e.md` — seed one vendor email E2E → moving 3D node. Includes mock-llm (keeps LOCKED CommBot untouched), `tools/seed` franz-go producer, and the P2P projection fix. **Grounded finding:** orchestrator_outbox has NO sequence_engine_key column (it's in the protobuf payload; `handleP2PCompleted` reads a non-existent `po_id`) → fix = ADDITIVE `sequence_engine_key INT8` column on orchestrator_outbox (Sentinel-authorized exception to the schema lock; do NOT touch the exactly-once CTE) + populate from workflow + viz reads `after.sequence_engine_key`.
  - `.github/workflows/e2e.yml` — GitHub Actions: build+vet both modules, then boot the stack (`docker compose up`), `docker compose wait crdb-init`==0, assert ≥3 running changefeeds, run seeder, assert seeded seqkey reaches viz SSE `/api/stream`. Needs repo secrets `CRDB_LICENSE` + `CRDB_ORG`; e2e job skips cleanly if absent.
  - `docs/antigravity/WEBSOCKET_UPGRADE_SPEC.md` — SPEC only; build AFTER E2E green. Single WS replacing SSE+REST-replay, coder/websocket, typed bidi envelope (replay/filter/presence), preserves HLC watermark ordering + no-gap/no-dup reconnect.
  - Run target chosen: **GitHub Actions**. Research: jezweb/claude-skills + AgriciDaniel/claude-obsidian evaluated, NOT adopted (web-focused / redundant with existing memory+ledger).
  - **Free-tier DevSecOps added** (cybersecurity showcase pillar): `e2e.yml` gained a `security` job (gosec SAST both modules + Trivy vuln/misconfig/secret scan, SARIF → Security tab, report-only for now); new `.github/workflows/codeql.yml` (GitHub CodeQL, Go). All free on public repos.
  - **Prompt 4 plan APPROVED** with 3 guardrails (CTE byte-for-byte + additive column only; mock-llm/seeder match CommBot's real contracts; no fabricated boot logs). Antigravity implementing next.
  - **Computer Use ruled out:** CLI Computer Use is macOS-only; user is on Windows 11. Manual paste-loop stands; Claude drives terminal + browser only.
- **DECISION PENDING:** transport stays SSE for now; user asked for WebSocket — flagged as a SEPARATE future audited prompt (do not fold into compose). BI = Next.js web dashboard (no Power BI).
- **Tooling added 2026-07-23:** repo `CLAUDE.md` (locked constraints + build/verify commands, auto-loads every session); Sentinel change-watcher `scratchpad/omniflow-watch.sh` (polls tree every 10s, wakes Claude only on real change — arm it LAST after own edits settle or it self-triggers).

## OPEN / residuals
1. protovalidate: fixed (vendored tree removed; `third_party/proto` include; BSR gen module in go.mod). buf.yaml modules = contracts/proto + third_party/proto.

## Cross-cutting backlog
human-approval event producer (admin → approval topic); lease-TTL reclaim (`owner_pod`/`lease_expires_at` written, never enforced); OTLP collector + Grafana/Tempo; K8s/Terraform/ArgoCD/Istio; `go test`/CI; Phase-4 restatement perf (replay-from-checkpoint not from 0). Recurring hygiene: delete Antigravity's `Architecture base N/` staging folders.

## Grade sheets / charters
`docs/audit/PHASE2_GRADE.md`, `PHASE3_GRADE.md`, `PHASE3_CHARTER.md`, `PHASE4_CHARTER.md`, `PHASE5_CHARTER.md`.
